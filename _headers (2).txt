/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: SAMEORIGIN
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), camera=(), microphone=()

# Embed pages are MEANT to be iframed on other people's sites (that's the
# whole point of the embeddable-widget feature) — so this path is
# deliberately excluded from the SAMEORIGIN rule above. Do not "fix" this
# by making it stricter, or embeds will silently stop working everywhere.
/embed/*
  X-Frame-Options: ALLOWALL

/assets/*
  Cache-Control: public, max-age=31536000, immutable

/*.html
  Cache-Control: public, max-age=3600, must-revalidate
